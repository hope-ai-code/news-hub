services:
  news-hub:
    build: .
    container_name: news-hub
    restart: unless-stopped
    ports:
      - "${NEWS_HUB_PORT:-2003}:5000"
    volumes:
      - ./data:/app/data
    env_file:
      - path: .env
        required: false
    environment:
      - TZ=${TZ:-Europe/Oslo}
